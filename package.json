{
  "name": "thinking-in-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "prop-types": "^15.5.10",
    "react": "^15.6.1",
    "react-dom": "^15.6.1"
  },
  "devDependencies": {
    "concurrently": "^3.5.0",
    "json-server": "^0.10.1",
    "react-scripts": "1.0.7"
  },
  "scripts": {
    "start": "./node_modules/.bin/concurrently \"./node_modules/.bin/json-server --watch db.json --port 3004\" \"react-scripts start\"",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
